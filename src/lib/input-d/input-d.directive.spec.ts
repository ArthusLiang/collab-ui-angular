import { InputDDirective } from './input-d.directive';

describe('InputDDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDDirective();
    expect(directive).toBeTruthy();
  });
});
